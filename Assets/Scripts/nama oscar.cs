using UnityEngine;

public class namaoscar : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Oscar");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
