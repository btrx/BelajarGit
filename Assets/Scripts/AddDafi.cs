using UnityEngine;

public class AddDafi : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Dafi added to the project!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
