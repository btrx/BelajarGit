using UnityEngine;

public class tambahnama : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama saya  arhaqifadilgaffari");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
