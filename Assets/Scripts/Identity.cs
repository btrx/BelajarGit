using UnityEngine;

public class Identity : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("nama saya neal");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
