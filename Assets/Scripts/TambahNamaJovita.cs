using UnityEngine;

public class TambahNamaJovita : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama saya Jovita");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
