using UnityEngine;

public class NamaGiga : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
      Debug.Log("Nama Saya Giga Wahyu Aditya");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
