using UnityEngine;

public class CallNama : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("NamakuBing");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
